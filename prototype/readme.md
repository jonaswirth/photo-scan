# Overview
This directory contains the algorithms for edge detection and document processing implemented in python.
The algorithms were developed in python before being implemented in JavaScript as it allows for a much simpler development flow.

## Requirements
The requirements are listed in `requirements.txt`. Install them by executing:
```
pip install -r "requirements.txt"
```
